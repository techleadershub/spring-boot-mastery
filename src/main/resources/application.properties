# Server configuration
server.port=8080
spring.application.name=api-key-auth

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:apikeysdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Logging configuration
logging.level.root=INFO
logging.level.com.example.apikeyauth=DEBUG
logging.level.org.springframework.security=DEBUG

# API Key Configuration
api.key.header.name=X-API-KEY
api.key.admin.value=ADMIN-API-KEY-SECRET-123

# For PostgreSQL (uncomment for production)
# spring.datasource.url=jdbc:postgresql://localhost:5432/apikeysdb
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect 